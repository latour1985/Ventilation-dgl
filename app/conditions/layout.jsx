// Titre de la page publique des conditions (la page elle-même est un
// composant client — le titre vit donc ici).
export const metadata = { title: "Termes et conditions" };

export default function ConditionsLayout({ children }) {
  return children;
}
